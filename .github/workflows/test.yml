name: E-Stack tests

on:
  push:
    branches:
      - camilladsp-4.1-estack
  pull_request:
    branches:
      - camilladsp-4.1-estack

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run repository and Measurement Batch tests
        run: npm test
