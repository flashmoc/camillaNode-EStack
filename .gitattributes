# Normalize text in Git while preserving binary files untouched.
* text=auto

# Shell launchers are executed inside the canonical Linux Dev Container.
*.sh text eol=lf

# Keep executable source, container metadata and canonical docs portable too.
*.js text eol=lf
*.json text eol=lf
*.md text eol=lf
